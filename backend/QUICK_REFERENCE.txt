╔═══════════════════════════════════════════════════════════════════════════════╗
║                    QUICK REFERENCE - SMART SEARCH API                          ║
╚═══════════════════════════════════════════════════════════════════════════════╝

PROBLEM FIXED:
══════════════════════════════════════════════════════════════════════════════════
✅ "Cannot find module './routes/smartSearch'" - RESOLVED
   → Created: backend/routes/smartSearch.js


THREE TERMINAL SETUP:
══════════════════════════════════════════════════════════════════════════════════

TERMINAL 1 - ML API:
  cd ml-ranking-system
  python -m uvicorn api:app --reload
  Port: 8000

TERMINAL 2 - Node Backend:
  cd backend
  node server
  Port: 5000

TERMINAL 3 - Testing:
  curl http://localhost:5000/api/ranking-status


API ENDPOINTS:
══════════════════════════════════════════════════════════════════════════════════

POST /api/search
  Body: {"query":"shoe", "budgetMin":1000, "budgetMax":1500}
  Returns: Ranked products with scores

POST /api/explain
  Body: {"productId":"...", "budgetMin":1000, "budgetMax":1500}
  Returns: Why product is ranked high

GET /api/ranking-status
  Returns: ML API health and status


CONFIGURATION:
══════════════════════════════════════════════════════════════════════════════════

File: backend/.env
─────────────────
RANKING_API_URL=http://localhost:8000
RANKING_ENABLED=true


TEST CURL COMMANDS:
══════════════════════════════════════════════════════════════════════════════════

1. Check ML API Health:
   curl http://localhost:8000/health

2. Check Backend Health:
   curl http://localhost:5000/api/ranking-status

3. Search Products:
   curl -X POST http://localhost:5000/api/search \
     -H "Content-Type: application/json" \
     -d '{"query":"shoe","budgetMin":1000,"budgetMax":1500}'

4. Explain Ranking:
   curl -X POST http://localhost:5000/api/explain \
     -H "Content-Type: application/json" \
     -d '{"productId":"60d5ec49...", "budgetMin":1000, "budgetMax":1500}'


EXPECTED RESPONSE (SEARCH):
══════════════════════════════════════════════════════════════════════════════════

{
  "success": true,
  "query": "shoe",
  "budget": {"min": 1000, "max": 1500},
  "totalProducts": 5,
  "results": [
    {
      "rank": 1,
      "productId": "...",
      "productName": "Nike Running Shoe",
      "price": 1200,
      "rating": 4.5,
      "reviews": 450,
      "rankingScore": 0.78,
      "valueScore": 0.75
    }
  ],
  "usingMLRanking": true
}


INTEGRATION IN REACT:
══════════════════════════════════════════════════════════════════════════════════

import axios from 'axios';

const handleSearch = async () => {
  try {
    const response = await axios.post('/api/search', {
      query: searchTerm,
      budgetMin: minBudget,
      budgetMax: maxBudget
    });
    
    setProducts(response.data.results);  // Already ranked!
  } catch (error) {
    console.error('Search failed:', error);
  }
};


TROUBLESHOOTING:
══════════════════════════════════════════════════════════════════════════════════

Error: Cannot find module            → ✅ FIXED - smartSearch.js created
Error: ECONNREFUSED (ML API)          → Start Python API first
Error: No products found             → Check MongoDB has data
Error: Invalid budget range           → Check budgetMin <= budgetMax
Error: axios not found               → npm install axios

If ML API unavailable:
  → Automatic fallback ranking enabled
  → System continues working
  → Quality × popularity ranking used


FILES CREATED:
══════════════════════════════════════════════════════════════════════════════════

✓ backend/routes/smartSearch.js      (290 lines, production-ready)
✓ backend/.env.example               (Configuration template)
✓ backend/RANKING_SETUP.md           (Detailed setup guide)
✓ backend/MODULE_FIX_SUMMARY.md      (This fix summary)


FEATURE SUMMARY:
══════════════════════════════════════════════════════════════════════════════════

✓ Intelligent ML Ranking         → Uses XGBoost learning-to-rank
✓ Fallback Mechanism             → Works without ML API
✓ Budget Enforcement             → Strict price filtering
✓ Text Search                    → Query-based filtering
✓ Category Filtering             → Optional cat parameter
✓ Ranking Scores                 → 0-1 confidence scores
✓ Explanations                   → Why is this ranked high?
✓ Health Checks                  → Service availability monitoring


═══════════════════════════════════════════════════════════════════════════════

🎯 READY TO GO!

Your backend is fixed and ready to serve intelligent product rankings.

Start: node server  (in backend folder)

═══════════════════════════════════════════════════════════════════════════════
